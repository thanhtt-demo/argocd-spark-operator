# =============================================================================
# Namespace: spark-operator
# Dedicated namespace for the Spark Operator controller and webhook.
# Isolates operator components from user Spark applications.
# Managed by ArgoCD (sync wave 1 - created first).
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: spark-operator
  labels:
    app.kubernetes.io/part-of: spark-platform     # Part of the spark-platform project
    app.kubernetes.io/managed-by: argocd          # Managed by ArgoCD GitOps
