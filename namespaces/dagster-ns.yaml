# =============================================================================
# Namespace: dagster
# Dedicated namespace for the Dagster orchestration platform.
# Components: webserver, daemon, user-code deployments, PostgreSQL.
# Managed by ArgoCD (sync wave 1 - created first).
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: dagster
  labels:
    app.kubernetes.io/part-of: spark-platform
    app.kubernetes.io/managed-by: argocd
---
# =============================================================================
# Namespace: keycloak
# Dedicated namespace for the Keycloak identity and access management platform.
# Components: Keycloak server, PostgreSQL.
# Managed by ArgoCD (sync wave 1 - created first).
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: keycloak
  labels:
    app.kubernetes.io/part-of: spark-platform
    app.kubernetes.io/managed-by: argocd
